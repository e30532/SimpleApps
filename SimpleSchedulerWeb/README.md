```
# su - db2inst1
$ db2 CREATE DATABASE scheddb USING CODESET UTF-8 TERRITORY en-us
DB20000I  The CREATE DATABASE command completed successfully.
$ vi /tmp/tmp.ddl
$ cat /tmp/tmp.ddl 
CREATE TABLE DB2INST1.MySchedulerTASK ("TASKID" BIGINT NOT NULL , "VERSION" VARCHAR(5) NOT NULL , "ROW_VERSION" INTEGER NOT NULL , "TASKTYPE" INTEGER NOT NULL , "TASKSUSPENDED" SMALLINT NOT NULL , "CANCELLED" SMALLINT NOT NULL , "NEXTFIRETIME" BIGINT NOT NULL , "STARTBYINTERVAL" VARCHAR(254) , "STARTBYTIME" BIGINT , "VALIDFROMTIME" BIGINT , "VALIDTOTIME" BIGINT , "REPEATINTERVAL" VARCHAR(254) , "MAXREPEATS" INTEGER NOT NULL , "REPEATSLEFT" INTEGER NOT NULL , "TASKINFO" BLOB(102400) LOGGED NOT COMPACT , "NAME" VARCHAR(254) NOT NULL , "AUTOPURGE" INTEGER NOT NULL ,"FAILUREACTION" INTEGER , "MAXATTEMPTS" INTEGER , "QOS" INTEGER ,"PARTITIONID" INTEGER , "OWNERTOKEN" VARCHAR(200) NOT NULL , "CREATETIME" BIGINT NOT NULL )  IN USERSPACE1;
ALTER TABLE DB2INST1.MySchedulerTASK ADD PRIMARY KEY ("TASKID");
CREATE INDEX DB2INST1.MySchedulerTASK_IDX1 ON DB2INST1.MySchedulerTASK ("TASKID", "OWNERTOKEN") ;
CREATE INDEX DB2INST1.MySchedulerTASK_IDX2 ON DB2INST1.MySchedulerTASK ("NEXTFIRETIME" ASC, "REPEATSLEFT", "PARTITIONID") CLUSTER;
CREATE TABLE DB2INST1.MySchedulerTREG ("REGKEY" VARCHAR(254) NOT NULL , "REGVALUE" VARCHAR(254) ) IN USERSPACE1;
ALTER TABLE DB2INST1.MySchedulerTREG ADD PRIMARY KEY ("REGKEY");
CREATE TABLE DB2INST1.MySchedulerLMGR ("LEASENAME" VARCHAR(254) NOT NULL, "LEASEOWNER" VARCHAR(254) NOT NULL, "LEASE_EXPIRE_TIME"  BIGINT, "DISABLED" VARCHAR(5))IN USERSPACE1;
ALTER TABLE DB2INST1.MySchedulerLMGR ADD PRIMARY KEY ("LEASENAME");
CREATE TABLE DB2INST1.MySchedulerLMPR ("LEASENAME" VARCHAR(254) NOT NULL, "NAME" VARCHAR(254) NOT NULL, "VALUE" VARCHAR(254) NOT NULL) IN USERSPACE1;
CREATE INDEX DB2INST1.MySchedulerLMPR_IDX1 ON DB2INST1.MySchedulerLMPR ("LEASENAME", "NAME");
$ db2 connect to SCHEDDB

   Database Connection Information

 Database server        = DB2/LINUXX8664 11.5.7.0
 SQL authorization ID   = DB2INST1
 Local database alias   = SCHEDDB

$ db2 -tf /tmp/tmp.ddl 
DB20000I  The SQL command completed successfully.
DB20000I  The SQL command completed successfully.
DB20000I  The SQL command completed successfully.
DB20000I  The SQL command completed successfully.
DB20000I  The SQL command completed successfully.
DB20000I  The SQL command completed successfully.
DB20000I  The SQL command completed successfully.
DB20000I  The SQL command completed successfully.
DB20000I  The SQL command completed successfully.
DB20000I  The SQL command completed successfully.
$
```

<img width="877" height="673" alt="image" src="https://github.com/user-attachments/assets/9e41aa79-3877-4266-ad84-57697e5582ea" />
